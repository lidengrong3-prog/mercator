# Mercator 部署修复报告

> 日期：2026-07-18 ｜ 阶段：代码审查后的部署与优化（阶段三）
> 范围：对《代码审查报告》中 14 个问题（P1×4 / P2×6 / P3×4）逐一排查、修复，并在部署验证阶段额外发现并修复了多处遗漏的 XSS 注入点。

---

## 一、验证结论总览

| 验证项 | 结果 |
|---|---|
| 前端 JS 语法校验（脚本块 464,626 字符） | ✅ PASS（Node `--check`） |
| XSS 防护运行时单元测试 | ✅ 20/20 PASS |
| `onclick` 原始字符串注入扫描 | ✅ 0 处残留（仅剩数值型索引，安全） |
| 本地服务器 HTTP | ✅ 200，815,476 字节 |
| Python 脚本语法校验 | ✅ py_compile 通过 |
| `migrate_data.py` 无凭证优雅退出 | ✅ 退出码 1，提示清晰 |

---

## 二、原审查问题修复详情

### P1-1｜内联事件属性 XSS 注入（严重）
- **根因**：政策标题、国家名、店铺名、分组名、平台档案、规则定位、报告名、产品详情等动态数据，在拼接到 `onclick="..."` 等内联事件属性时仅做了单引号转义（`'`），未转义 `< > " &`，攻击者可构造 `" onmouseover=...` 或注入 `</button><script>` 突破属性边界执行脚本（存储型 XSS）。
- **解决方案**：
  1. 新增两个安全工具函数（`index.html` ~3448 行）：
     - `escapeHtml(s)`：转义 `& < > " '`，用于插入 `innerHTML` 文本。
     - `escInline(s)`：先 JS 字符串转义再 HTML 属性转义，专用于内联事件属性中的动态值（浏览器解析属性时会还原为原值，接收函数拿到的仍是正确的原始值）。
  2. 对全部识别出的注入点套用 `escInline` / `escapeHtml`，覆盖政策、国家、店铺、分组、平台、规则、报告、产品等模块。
- **验证**：XSS 运行时测试 20 项全过；`onclick` 扫描 0 残留。

### P1-2｜演示模式认证绕过（严重）
- **根因**：演示模式（无 Supabase 时兜底）下，任意邮箱 + 任意密码即可登录，且直接授予 `tier: 'pro'`，无邮箱格式与密码强度校验。
- **解决方案**（`doLogin` / `doRegister`，~7640 行）：
  - 邮箱正则 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` 校验；
  - 密码长度 `< 6` 拦截（实际代码为 `(password||'').length < 6`）；
  - 演示会话默认 `tier: 'free'`，杜绝越权 Pro 能力。
- **验证**：源码标记 `tier: 'free'`、`emailOk` 校验、密码长度校验均确认存在；HTTP 内容标记 OK。

### P1-3｜数据库缺索引（性能）
- **根因**：`user_watchlist`、`user_activity`、`query_history`、`generated_reports`、`feedback`、`watchlist_items` 等表的高频查询字段（`user_id`、`created_at`、`status`、`item_type`）无索引，随数据量增长查询会全表扫描。
- **解决方案**：新建 `supabase/add_indexes.sql`（幂等 `IF NOT EXISTS`，共 7 条索引），覆盖 `user_id`、`(user_id, item_type)`、`(user_id, created_at DESC)`、`(status)` 等组合。
- **部署说明**：需在 Supabase 执行该 SQL；当前无凭证环境不自动执行，属预期（详见第四节「无法自动处理项」）。

### P1-4｜迁移脚本配置与依赖（可维护/安全）
- **根因**：`migrate_data.py` 使用占位符凭证且依赖 `httpx`，但无 `requirements.txt`，本地无法运行且易误提交密钥。
- **解决方案**：
  - 凭证改为从环境变量 `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` 读取；
  - `httpx` 替换为标准库 `urllib`，消除第三方依赖；
  - 新增 `requirements.txt` 声明零第三方依赖；
  - 无凭证时优雅退出并提示正确配置方式（已验证退出码 1）。
- **验证**：py_compile 通过；无凭证运行返回明确中文提示。

### P2-1 / P2-2｜错误处理与查询编码
- **P2-1 根因**：`loadMercatorProfile` 无 try-catch，Supabase 异常会冒泡导致白屏。
- **P2-1 方案**：包 try-catch，`r.error` 显式抛出，失败使用默认 profile（~7587 行，已确认）。
- **P2-2 根因**：PostgREST 查询参数（`key`、`id`、`user_id`）直接拼接，含特殊字符会破坏请求。
- **P2-2 方案**：全部改用 `encodeURIComponent(...)` 编码（共 7 处）。

### P2-3 / P2-6 / P3-4｜事件监听、定时器、防重入
- **P2-3 根因**：AI Advisor 全局 `click` 监听使用匿名函数，重复初始化会累积多个监听器（内存泄漏 + 行为异常）。
- **P2-3 方案**：改为具名函数 `aiAdvisorCloseOnOutsideClick`，可正确移除。
- **P2-6 根因**：实时快讯轮播 `setInterval` 不随页面切换暂停，后台空转耗电。
- **P2-6 方案**：新增 `visibilitychange` 监听，页面隐藏时暂停、可见时恢复。
- **P3-4 根因**：报告生成按钮可重复点击触发并发生成。
- **P3-4 方案**：新增模块级 `rpGenInterval`，生成中再次点击直接 return（防重入）。

### P2-5 / P3-1 / P3-2｜Python 脚本健壮性
- **P2-5**：抽象 `_search_html_site()` / `_extract_title()` 通用函数，消除 amz123 / cifnews 搜索逻辑重复。
- **P3-1**：`bare except:` 改为 `except UnicodeDecodeError:` / `except LookupError:`。
- **P3-2**：`merge_data()` 的 `json.load` 加 try-catch（JSONDecodeError/ValueError/OSError），损坏文件自动备份为 `.corrupt.<时间戳>` 并重建。

---

## 三、验证阶段额外发现的 XSS 注入点（已修复）

原审查（P1-1）聚焦于主要模块，但自动化扫描（`scripts/onclick_scan.js`，对 39 个动态 `onclick` 处理器全量检查）又发现以下**未转义的动态字符串拼接**：

| 位置（行） | 注入值 | 风险 | 处置 |
|---|---|---|---|
| 6434 | 用户自建**文件夹名** `f`（用户可控） | 存储型 XSS（高危） | `data-folder` 用 `escapeHtml(f)`，`onclick` 用 `escInline(f)`，可见文本用 `escapeHtml(f)` |
| 6462 | 当前文件夹 `ctActiveFolder`（用户可控） | 存储型 XSS（高危） | `escInline(ctActiveFolder)` |
| 4519 / 4520 / 4532 | 国家键 `key`、国家名 `d.name`（数据驱动） | 防御性纵深 | `escInline(key)` / `escInline(d.name)` |
| 6375 | 推荐名 `cr.name`（数据驱动） | 防御性纵深 | `escInline(cr.name)` |
| 8424 | 标签键 `t.k`、标签名 `t.l`（数据驱动） | 防御性纵深 | `escInline(t.k)` / `escapeHtml(t.l)` |
| 8472–8474 | 预警 `id`（数据驱动） | 防御性纵深 | `escInline(id)` |
| 8698 | 成员邮箱 `m.email`（数据驱动） | 防御性纵深 | `escInline(m.email)` |
| 6976 / 6977 / 6983 | 素材 `m.id`（用户添加内容） | 存储型 XSS（中危） | `data-id` 与事件处理器均 `escInline(m.id)` |

**说明**：`escInline` 仅做转义，不改变接收函数拿到的原始值（浏览器解析属性时自动还原），因此上述修改不影响任何业务逻辑，纯粹是安全加固。

仅剩的 `onclick` 拼接均为数值型索引（`idx`、`i`、`r.id`、`globalIdx` 等）——数字不可能突破属性边界或注入脚本，属安全范畴，无需转义。

---

## 四、无法自动处理 / 需后续人工介入项

1. **数据库索引与 RLS 策略上线**：`supabase/add_indexes.sql` 需在拥有 `service_role` 权限的 Supabase 项目执行；本地无凭证，未自动执行（符合预期）。请提供凭证后运行 `psql` 或 Supabase SQL Editor 应用。
2. **`migrate_data.py` 实际数据迁移**：同上，需配置 `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` 环境变量后执行，方可将本地 JSON 数据写入云端表。
3. **`innerHTML` 文本型数据注入**：本次重点修复了脚本执行向量（内联事件属性）。应用中仍有部分 `innerHTML = ... + dataField` 直接拼接文本（非事件属性）的场景，理论上恶意数据仍可能注入 HTML。建议后续对 `innerHTML` 拼接统一改用 `textContent` 或 `escapeHtml` 包裹——已作为独立优化项记录，未在本轮强制改动以避免影响既有渲染结构。
4. **真实浏览器端到端验证**：受沙箱限制，未启动真实浏览器做点击/表单交互验证；已通过 Node 语法校验、运行时单测、内容标记与 HTTP 探测替代确认。

---

## 五、遗留验证脚本（可复用）

| 脚本 | 用途 |
|---|---|
| `scripts/xss_verify.js` | 提取并断言 `escapeHtml` / `escInline` 的运行时正确性（20 项） |
| `scripts/onclick_scan.js` | 全量扫描 `onclick` 属性，检出未转义的动态字符串拼接 |
| `scripts/js_syntax_check.js` | 抽取所有 `<script>` 块做 `node --check` 语法校验 |
