# Mercator 项目代码审查报告

> 审查时间：2026-07-18
> 审查范围：index.html（8737 行前端 JS）、supabase/（数据库 schema + 认证模块）、scripts/collect_data.py、supabase/migrate_data.py
> 审查维度：功能完整性、逻辑漏洞、性能瓶颈、边界处理、代码重复、数据库效率、资源管理、参数校验、错误处理

---

## 一、审查概述

| 维度 | 文件 | 规模 | 问题数 |
|------|------|------|--------|
| 前端业务逻辑 | index.html | 8737 行 | 9 |
| 数据库设计 | schema.sql + phase2_schema.sql | 278 行 | 2 |
| 数据收集脚本 | scripts/collect_data.py | 1464 行 | 3 |
| 数据迁移脚本 | supabase/migrate_data.py | 200 行 | 2 |

**问题严重程度分级统计**：P1（高）4 项 · P2（中）6 项 · P3（低）4 项

---

## 二、P1 高优先级问题（建议立即修复）

### P1-1 ｜ XSS 注入风险：内联事件拼接动态数据且转义不完整

- **影响范围**：全站多处动态渲染（国家政策、店铺、平台详情、内容卡片）
- **严重程度**：高 — 攻击者可通过篡改数据源（Supabase market_data 或本地 JSON）注入恶意脚本
- **代码位置**：
  - `index.html:4591` — `onclick="toast('已同步至预警中心：'+p.title.replace(/'/g,"\\'")+')"`
  - `index.html:4592` — `onclick="alert('【高危政策提醒】...'+p.title.replace(/'/g,"\\'")+'...')"`
  - `index.html:4669` — `onclick="switchPage('products');toast('跳转产品雷达筛选: '+c[0]+'')"`
  - `index.html:4066` — `onclick="...value='"+s[0].substring(0,8)+"'..."`
- **问题分析**：动态数据（政策标题 `p.title`、店铺名 `s[0]`、品类名 `c[0]`）被直接拼入 `onclick` 内联属性，仅对单引号做了 `.replace(/'/g,"\\'")` 转义，**未转义 `<`、`>`、`"`、`&`、反引号**。若数据源含 `');alert('xss` 或 HTML 标签，可导致脚本执行。
- **优化建议**：
  1. 放弃内联 `onclick` 字符串拼接，改用 `addEventListener` + `dataset` 传递参数
  2. 若必须内联，使用 `encodeURIComponent` + 运行时 `decodeURIComponent`，并对 HTML 特殊字符做完整转义
  3. 对所有来自外部数据源的文本统一过 `escapeHtml()` 工具函数后再插入 innerHTML

### P1-2 ｜ 演示模式认证绕过：无密码强度校验、任意凭证可登录

- **影响范围**：未配置 Supabase 或 SDK 加载失败时的全部认证流程
- **严重程度**：高 — 演示模式下任意邮箱+任意密码即可获得 `pro` 权限
- **代码位置**：
  - `index.html:7610-7618`（doLogin 演示分支）
  - `index.html:7629-7638`（doRegister 演示分支）
- **问题分析**：
  ```js
  // doLogin 演示模式：仅判断非空即放行，密码无长度/复杂度要求
  if (email && password) {
    mercatorProfile = { ... tier: 'pro', ... };  // 直接授予 pro 权限
    localStorage.setItem('mercator_demo', ...);
    onAuthSuccess();
  }
  ```
  - 无邮箱格式校验（`a@b` 即可通过）
  - 无密码最小长度要求（单字符密码可通过）
  - 演示模式直接授予 `pro` 付费等级，可能误导权限控制逻辑
- **优化建议**：
  1. 演示模式至少校验邮箱格式 `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`
  2. 密码最小长度 6 位（与 Supabase 默认策略一致）
  3. 演示模式 tier 应为 `free` 或 `demo`，非 `pro`
  4. 演示会话加过期时间，避免长期有效

### P1-3 ｜ 数据库缺少关键索引：高频查询字段未建索引

- **影响范围**：所有用户数据查询（watchlist、activity、preferences、reports）
- **严重程度**：高 — 用户量增长后查询性能急剧下降
- **代码位置**：`supabase/schema.sql` + `supabase/phase2_schema.sql` 全文
- **问题分析**：以下表的高频查询字段**均未创建索引**：
  | 表名 | 缺失索引字段 | 典型查询 |
  |------|------------|---------|
  | user_watchlist | user_id, (user_id, item_type) | `?user_id=eq.xxx` |
  | user_activity | user_id, created_at | `WHERE user_id=? ORDER BY created_at` |
  | user_preferences | user_id（PK，已有） | — |
  | query_history | user_id, created_at | 按时间倒序查询 |
  | watchlist_items | user_id, item_type | 组合查询 |
  | reports | user_id, created_at | 按时间倒序 |
  | feedback | user_id, status | 按状态筛选 |
- **优化建议**：
  ```sql
  CREATE INDEX idx_watchlist_user ON user_watchlist(user_id);
  CREATE INDEX idx_watchlist_user_type ON user_watchlist(user_id, item_type);
  CREATE INDEX idx_activity_user_time ON user_activity(user_id, created_at DESC);
  CREATE INDEX idx_qh_user_time ON query_history(user_id, created_at DESC);
  CREATE INDEX idx_reports_user_time ON reports(user_id, created_at DESC);
  CREATE INDEX idx_feedback_status ON feedback(status);
  ```

### P1-4 ｜ migrate_data.py 占位符配置 + 依赖未声明

- **影响范围**：数据迁移脚本无法直接运行
- **严重程度**：高 — 运维人员需手动改源码，易出错且不安全
- **代码位置**：`supabase/migrate_data.py:13-14`、`supabase/migrate_data.py:8`（`import httpx`）
- **问题分析**：
  ```python
  SUPABASE_URL = "PLACEHOLDER_URL"
  SERVICE_ROLE_KEY = "PLACEHOLDER_KEY"
  ```
  - 凭证硬编码在源码中（service_role key 是高危凭证，能绕过 RLS）
  - 使用 `httpx` 但项目无 `requirements.txt`，运行时报 ModuleNotFoundError
  - 与 `collect_data.py`（用标准库 `urllib`）HTTP 客户端不一致
- **优化建议**：
  1. 改为从环境变量读取：`SUPABASE_URL = os.environ.get('SUPABASE_URL')`
  2. 统一使用标准库 `urllib`（与 collect_data.py 一致），或添加 `requirements.txt`
  3. service_role key 绝不应出现在代码仓库中

---

## 三、P2 中优先级问题（建议尽快修复）

### P2-1 ｜ 异步错误处理不完善：关键函数无 try-catch、fire-and-forget

- **影响范围**：登录后用户档案加载、登录时间记录
- **严重程度**：中 — 网络异常时可能导致未捕获的 Promise rejection
- **代码位置**：
  - `index.html:7562-7566`（loadMercatorProfile 无 try-catch）
    ```js
    async function loadMercatorProfile() {
      var r = await supabaseClient.from('profiles').select('*').eq('id', mercatorUser.id).single();
      // 若查询失败，异常向上传播，onAuthSuccess 不会执行
      mercatorProfile = r.data || {...};
    }
    ```
  - `index.html:7624`（fire-and-forget，无 await 无 catch）
    ```js
    supabaseClient.from('profiles').update({ last_login_at: ... }).eq('id', mercatorUser.id);
    // Promise 未 await 也未 .catch()，rejection 被静默吞掉
    ```
- **优化建议**：loadMercatorProfile 加 try-catch 并提供默认 profile；last_login_at 更新加 `.then().catch()` 或 `await` + try-catch

### P2-2 ｜ PostgREST 查询参数直接拼接：未做 URL 编码

- **影响范围**：watchlist 删除、用户偏好更新
- **严重程度**：中 — 若 ID 含特殊字符可能破坏查询语义
- **代码位置**：
  - `index.html:7760` — `'/user_watchlist?id=eq.' + watchId`
  - `index.html:7794` — `'/user_preferences?user_id=eq.' + mercatorUser.id`
  - `index.html:7500` — `'/market_data?key=eq.' + key`
- **问题分析**：watchId 来自数据库（UUID，风险低），但 `key` 和未来扩展的用户输入若含空格、`&`、`=` 会破坏 PostgREST 查询。
- **优化建议**：使用 `encodeURIComponent(key)` 对所有动态拼接的查询值编码，或改用 Supabase SDK 的 `.eq()` 方法

### P2-3 ｜ 事件监听器累积：全局 document.addEventListener 无法移除

- **影响范围**：搜索下拉、AI 顾问弹窗、用户菜单
- **严重程度**：中 — 单页应用中反复绑定导致回调重复触发
- **代码位置**：
  - `index.html:4848` — `document.addEventListener('click', function(e){...})`（搜索下拉关闭）
  - `index.html:8134` — `document.addEventListener('click', function(e){...})`（AI 顾问弹窗关闭）
  - `index.html:7917` — `document.addEventListener('click', function(e){...})`（用户菜单关闭）
- **问题分析**：这些全局监听器在初始化时绑定，若相关组件被重复初始化（如 SPA 路由切换），监听器会累积。由于使用匿名函数，无法 `removeEventListener`。
- **优化建议**：使用具名函数 + `{ once: false }`，在组件销毁时移除；或用事件委托集中管理

### P2-4 ｜ 凭证硬编码在前端：Supabase URL 与 anon key 明文

- **影响范围**：前端安全
- **严重程度**：中（Supabase anon key 设计上是公开的，依赖 RLS 保护）
- **代码位置**：`index.html:3194-3195`
  ```js
  var MERCATOR_SUPABASE_URL = 'https://ftlzofrnosgvdvwajhuz.supabase.co';
  var MERCATOR_SUPABASE_KEY = 'sb_publishable_y2zfDKmuW9Lj4gUqIYKpxw_COuX1JQQ';
  ```
- **问题分析**：anon key 公开是 Supabase 的设计，但需确保 RLS 策略严格。当前 `market_data` 表 `SELECT USING (true)` 允许匿名读取所有市场数据（符合设计），但需确认无敏感数据泄露。
- **优化建议**：确认 market_data 中无用户私有数据；考虑在构建时通过环境变量注入而非硬编码

### P2-5 ｜ 重复代码：collect_data.py 搜索函数高度重复

- **影响范围**：可维护性
- **严重程度**：中 — 6 个搜索函数逻辑几乎相同
- **代码位置**：`collect_data.py` 第 729-803 行（`_search_amz123`、`_search_cifnews`、`_search_federal_register`）
- **问题分析**：三个函数都是「构造 URL → fetch_html → 多正则匹配 → 去重 → 返回标题列表」，差异仅在 URL 模板和正则。`migrate_data.py` 的 4 个 migrate 函数也几乎相同。
- **优化建议**：抽象为 `search_site(url_builder, patterns, alias)` 通用函数；migrate 函数抽象为 `migrate_dataset(key, file, meta_builder)`

### P2-6 ｜ 实时快讯定时器边界：页面切换后未暂停

- **影响范围**：后台标签页性能
- **严重程度**：中 — setInterval 持续运行消耗资源
- **代码位置**：`index.html:8038` — `tickerInterval=setInterval(highlight,4000)`
- **问题分析**：实时快讯轮播 `setInterval` 每 4 秒执行 `scrollIntoView`，即使用户切到其他页面或标签页不可见时仍在运行。虽然展开/收起时有 clearInterval，但页面切换（switchPage）未暂停。
- **优化建议**：监听 `visibilitychange` 事件，标签页隐藏时暂停定时器；switchPage 时检查快讯面板是否可见

---

## 四、P3 低优先级问题（建议择机优化）

### P3-1 ｜ bare except 捕获所有异常

- **代码位置**：`collect_data.py:61` — `except: continue`
- **问题分析**：bare `except:` 会捕获 `KeyboardInterrupt`、`SystemExit`，可能导致 Ctrl+C 无法中断
- **优化建议**：改为 `except UnicodeDecodeError: continue`

### P3-2 ｜ merge_data 无文件损坏容错

- **代码位置**：`collect_data.py:652-653`
  ```python
  with open(existing_file, 'r', encoding='utf-8') as f:
      existing = json.load(f)  # 若文件损坏（非法 JSON）直接崩溃
  ```
- **优化建议**：加 try-catch，JSON 解析失败时重建空数据结构并备份损坏文件

### P3-3 ｜ console.log 调试代码残留

- **代码位置**：index.html 共 19 处 `console.log/warn`（如 3214、4980、7506 等）
- **问题分析**：生产环境会向控制台泄露内部状态和加载逻辑
- **优化建议**：构建时通过工具移除，或封装为可关闭的 debug 函数

### P3-4 ｜ 报告生成 setInterval 重复创建风险

- **代码位置**：`index.html:7025` — 报告生成动画 `setInterval`
- **问题分析**：用户快速重复点击"生成报告"可能创建多个 interval（虽有 `progress>=100` 时 clearInterval，但首次点击未做防重入）
- **优化建议**：生成开始时禁用按钮，或用 `if(interval) clearInterval(interval)` 防重入

---

## 五、优化优先级建议

| 优先级 | 问题编号 | 建议处理时间 |
|--------|---------|------------|
| **立即** | P1-1 (XSS), P1-2 (认证绕过) | 上线前必须修复 |
| **本周** | P1-3 (数据库索引), P1-4 (迁移配置) | 用户量增长前修复 |
| **本月** | P2-1 ~ P2-3 (错误处理/拼接/事件) | 代码质量提升 |
| **择机** | P2-4 ~ P2-6, P3 全部 | 技术债清理 |

---

## 六、总体评价

项目作为**静态单页应用 + Supabase BaaS** 架构整体合理，数据层的本地 JSON 兜底机制设计良好（保证了离线可用性），RLS 策略覆盖完整。主要风险集中在：

1. **前端安全**：XSS 转义不完整 + 演示模式认证宽松，是上线前最高优先级
2. **数据库性能**：索引缺失会在用户量增长后成为瓶颈
3. **代码质量**：重复代码和错误处理不完善影响长期维护性

建议优先处理 P1 级别的 4 个问题，其中 P1-1 和 P1-2 涉及安全，应在任何公开部署前修复。
